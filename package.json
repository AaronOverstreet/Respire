{
  "name": "respire-site-workspace",
  "private": true,
  "workspaces": ["respire-site"],
  "scripts": {
    "dev": "npm run dev -w respire-site",
    "build": "npm run build -w respire-site",
    "preview": "npm run preview -w respire-site"
  }
}
